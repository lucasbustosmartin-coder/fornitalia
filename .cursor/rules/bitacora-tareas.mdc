---
description: Actualizar bitácora Excel (Log y Resumen) al completar tareas; flujo de despliegue
alwaysApply: true
---

# Bitácora de tareas – Fornitalia

**Por defecto:** siempre actualizar la bitácora (Log y, si aplica, Resumen) al completar una tarea. No saltear esta regla salvo que el usuario lo indique explícitamente (por ejemplo: "esto no agregarlo a la bitácora", "no lo registres", "no actualices la bitácora").

Al completar una tarea relevante en este proyecto:

1. **Solapa Log**  
   Agregar una fila en el array `datosLog` de `crear-bitacora-excel.js` con: Fecha, Hora, titulo_tarea, desc_tarea, etapa.  
   Usar cuando la tarea sea un trabajo realizado (cambio, feature, corrección).

2. **Solapa Resumen**  
   Si la tarea agrega o cambia una **funcionalidad de la app** (dashboard, flujo de caja, etc.), agregar (o actualizar) la fila correspondiente en el array `funcionalidades` de `crear-bitacora-excel.js` con: Funcionalidad, Descripción.

3. **Solapa Presupuesto**  
   Solo cuando el usuario pida ajustar o ampliar el **presupuesto comercial** (rubros de trabajo y montos), actualizar el array `presupuesto` de `crear-bitacora-excel.js` con filas de la forma: Grupo, Descripción comercial, Importe sugerido (ARS). Los importes son orientativos para Argentina.

4. **Regenerar Excel**  
   Después de editar el script, ejecutar:  
   `node crear-bitacora-excel.js`  
   para que `Bitacora_tareas.xlsx` tenga actualizadas las solapas Log, Resumen, Versiones y Presupuesto.

Archivo a editar: `crear-bitacora-excel.js`.  
Salida: `Bitacora_tareas.xlsx`.

---

# Flujo al final de cada tarea (despliegue)

Al terminar una tarea:

1. **El usuario prueba en local** y valida que todo funcione.
2. **El usuario confirma** (por ejemplo: "listo", "confirmo", "desplegá", "subilo").
3. **Recién entonces** el asistente despliega:
   - `git add .` → `git commit -m "descripción breve"` → `git push origin main`
   - Vercel redepliega automáticamente al hacer push.
4. **Registrar versión incremental** en la bitácora: agregar una fila en el array `versiones` de `crear-bitacora-excel.js` (Versión, Fecha, Descripción del despliegue), incrementar el número de versión, y ejecutar `node crear-bitacora-excel.js`. Luego hacer commit y push de la bitácora actualizada (si no se incluyó ya en el mismo commit del punto 3).

No hacer push a Git / no asumir despliegue hasta que el usuario confirme después de probar en local.
